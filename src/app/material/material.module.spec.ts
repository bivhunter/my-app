import { MaterialModule } from './material.module';

describe('MaterialModule', () => {
  const module: MaterialModule = new MaterialModule();

  it('should create', () => {
    expect(module).toBeTruthy();
  });
});
